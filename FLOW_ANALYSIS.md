# 📋 Анализ основного флоу HR Skip Bot

## ✅ Что реализовано:

### 1. Пользователь заходит в Telegram бота ✅
- Команда `/start` работает
- Создание пользователя в БД
- Главное меню с кнопками

### 2. Авторизация на HH.ru ❌ **НЕ РЕАЛИЗОВАНО**
- ❌ Нет OAuth эндпоинта `/auth/callback`
- ❌ Нет кнопки авторизации в боте
- ❌ Нет сохранения токенов доступа

### 3. Получение рекомендованных вакансий ⚠️ **ЧАСТИЧНО**
- ✅ Метод `getRecommendedVacancies` есть
- ⚠️ Требует `user.resume.hhId` (нужна авторизация HH.ru)
- ✅ Есть fallback на поиск по ключевым словам

### 4. ИИ агент находит компании ✅
- ✅ Метод `searchJobOnCompanyWebsite` реализован
- ✅ Использует Puppeteer для парсинга сайтов
- ✅ Использует OpenAI для анализа

### 5. Поиск контактов HR/руководителей ✅
- ✅ Метод `analyzeContactInfo` реализован
- ✅ Анализ email, телефонов, форм на сайте

### 6. Отправка CV и сопроводительного письма ⚠️ **ЧАСТИЧНО**
- ✅ Метод `sendApplicationEmail` есть
- ⚠️ Email отправляется от `user.email`, но SMTP использует ваш email
- ⚠️ Нет загрузки CV файла пользователя
- ✅ Сопроводительное письмо берется из шаблона пользователя

## ❌ Что нужно добавить:

### 1. OAuth авторизация HH.ru
- Добавить эндпоинт `/auth/callback` в Express
- Добавить кнопку авторизации в боте
- Сохранять access_token и refresh_token в БД

### 2. Загрузка CV пользователя
- Команда `/resume` для загрузки файла
- Сохранение файла в `uploads/`
- Прикрепление к email

### 3. Исправление отправки email
- Использовать SMTP_USER как отправителя
- Reply-To указывать на user.email
- Или настроить отправку от имени пользователя (требует его SMTP)

## 📝 Рекомендуемый флоу:

1. **Пользователь:** `/start` → Бот приветствует
2. **Авторизация:** Кнопка "🔑 Авторизоваться на HH.ru" → OAuth flow → Сохранение токенов
3. **Настройка:** `/settings` → Email, ключевые слова, шаблон письма
4. **Загрузка CV:** `/resume` → Загрузка файла PDF/DOCX
5. **Поиск:** `/search` → Получение вакансий через HH API
6. **Применение:** Кнопка "📤 Отправить" → ИИ ищет контакты → Отправка email

## 🔧 Приоритеты доработки:

1. **КРИТИЧНО:** OAuth авторизация HH.ru
2. **ВАЖНО:** Загрузка CV файла
3. **ВАЖНО:** Исправление отправки email (использовать ваш SMTP, но Reply-To на user.email)
